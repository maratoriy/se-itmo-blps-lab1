spring:
  application:
    name: se-itmo-blps-lab1

  jpa:
    hibernate:
      ddl-auto: validate

  datasource:
    url:
    username:
    password:

  liquibase:
    change-log: classpath:changelog.xml